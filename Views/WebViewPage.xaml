<Page 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Custom="using:Microsoft.UI.Xaml.Controls" NavigationCacheMode="Enabled"
    x:Class="Phasmaphobia_Editor.Views.WebViewPage"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">
    <Page.Resources>
        <BitmapImage x:Key="tanglewood" UriSource="/Assets/Tanglewood.png" />
        <BitmapImage x:Key="ridgeview" UriSource="/Assets/ridgeview.png" />
        <BitmapImage x:Key="willo" UriSource="/Assets/willow.png" />
        <BitmapImage x:Key="edgefield" UriSource="/Assets/edgefield.png" />
        <BitmapImage x:Key="bleasdale" UriSource="/Assets/bleasdale.png" />
        <BitmapImage x:Key="woodwing" UriSource="/Assets/woodwing.png" />
        <BitmapImage x:Key="grafton" UriSource="/Assets/grafton.png" />
        <BitmapImage x:Key="prison" UriSource="/Assets/prison.png" />
        <BitmapImage x:Key="brownstone" UriSource="/Assets/brownstone.png" />
        <BitmapImage x:Key="maple" UriSource="/Assets/maple.png" />
    </Page.Resources>
    <Grid x:Name="ContentArea" Margin="0,0,0,0" Loaded="ContentArea_Loaded">
        <Image x:Name="dark_banner" Height="100" Width="216" Margin="32,72,0,0" Source="/Assets/dark.png" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <Button Content="6 Tanglewood Dr" Height="42" Width="226" Margin="27,183,0,0" VerticalAlignment="Top" >
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                                               PropertyName="Source"
                                               Value="{StaticResource tanglewood}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="10 Ridgeview Ct" Height="42" Width="226" Click="Button_Click" Margin="27,232,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                                   PropertyName="Source"
                                   Value="{StaticResource ridgeview}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="13 Willow St" Height="42" Width="226" Click="Button_Click_1" Margin="27,283,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                                   PropertyName="Source"
                                   Value="{StaticResource willo}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="42 Edgefield Rd" Height="42" Width="226" Margin="27,334,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                                   PropertyName="Source"
                                   Value="{StaticResource edgefield}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="Bleasdale Farmhouse" Height="42" Width="226" Margin="27,385,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                                   PropertyName="Source"
                                   Value="{StaticResource bleasdale}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="Camp Woodwind" Height="42" Width="226" Margin="27,436,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                                   PropertyName="Source"
                                   Value="{StaticResource woodwing}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="Grafton Farmhouse" Height="42" Width="226" Margin="27,487,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                                   PropertyName="Source"
                                   Value="{StaticResource grafton}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="Maple Lodge Campsite" Height="42" Width="226" Click="Button_Click_2" Margin="27,538,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                                   PropertyName="Source"
                                   Value="{StaticResource maple}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="Prison" Height="42" Width="226" Margin="27,589,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                                   PropertyName="Source"
                                   Value="{StaticResource prison}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="Brownstone High School" Height="42" Width="226" Click="Button_Click_3" Margin="27,640,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                                   PropertyName="Source"
                                   Value="{StaticResource brownstone}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Image x:Name="light_banner" Height="100" Width="216" Margin="32,72,0,0" Source="/Assets/light.png" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <Image x:Name="map" Margin="267,72,36,86" Source="{StaticResource tanglewood}"/>
        <Button Content="Sunny Meadows" Height="42" Width="226" Margin="27,691,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                        PropertyName="Source"
                        Value="{StaticResource grafton}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="Sunny Meadows R: Fem Wing" Height="42" Width="226" Click="Button_Click_2" Margin="27,794,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                        PropertyName="Source"
                        Value="{StaticResource maple}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="Sunny Meadows R: Male Wing" Height="42" Width="226" Margin="27,845,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                        PropertyName="Source"
                        Value="{StaticResource prison}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="Sunny Meadows R: Res Wing" Height="42" Width="226" Click="Button_Click_3" Margin="27,896,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                        PropertyName="Source"
                        Value="{StaticResource brownstone}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="Sunny Meadows R: Hospital Wing" Height="42" Width="226" Click="Button_Click_3" Margin="27,946,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                        PropertyName="Source"
                        Value="{StaticResource brownstone}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
        <Button Content="Sunny Meadows R" Height="42" Width="226" Margin="27,741,0,0" VerticalAlignment="Top">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:ChangePropertyAction TargetObject="{Binding ElementName=map}"
                        PropertyName="Source"
                        Value="{StaticResource grafton}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>
    </Grid>
</Page>
